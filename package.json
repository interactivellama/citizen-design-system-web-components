{
	"name": "citizen-components",
	"version": "0.1.0",
	"dependencies": {
		"elix": "^12.0.0"
	},
	"scripts": {
		"deploy": "npm install && rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist",
    "start": "npx serve"
	}
}
